const bcrypt=require('bcryptjs'); const jwt=require('jsonwebtoken'); const User=require('../models/User'); const {jwtSecret,jwtExpiresIn,superAdminEmail}=require('../config/env');
const safe=u=>({id:u._id,name:u.name,email:u.email,role:u.role,status:u.status});
function token(u){return jwt.sign({sub:u._id.toString(),role:u.role},jwtSecret,{expiresIn:jwtExpiresIn});}
async function signup(req,res){const {name,email,password}=req.body;if(!name||!email||!password||password.length<8)return res.status(400).json({message:'Name, email and password (8+ chars) are required'});const normalized=email.toLowerCase().trim();const existing=await User.findOne({email:normalized});if(existing)return res.status(409).json({message:'Email already registered'});const role=normalized===superAdminEmail?'SUPER_ADMIN':'USER';const passwordHash=await bcrypt.hash(password,12);const u=await User.create({name,email:normalized,passwordHash,role});res.status(201).json({token:token(u),user:safe(u)});}
async function login(req,res){const {email,password}=req.body;if(!email||!password)return res.status(400).json({message:'Email and password are required'});const u=await User.findOne({email:email.toLowerCase().trim()});if(!u||!(await bcrypt.compare(password,u.passwordHash)))return res.status(401).json({message:'Invalid email or password'});if(u.status!=='ACTIVE')return res.status(403).json({message:'Account disabled'});if(u.email===superAdminEmail&&u.role!=='SUPER_ADMIN'){u.role='SUPER_ADMIN';await u.save();}res.json({token:token(u),user:safe(u)});}
async function me(req,res){res.json({user:safe(req.user)});}
module.exports={signup,login,me};
