import {createContext,useContext,useEffect,useState} from 'react';import {api} from '../services/api';
const C=createContext();export function AuthProvider({children}){const [user,setUser]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{if(!localStorage.getItem('ft_token'))return setLoading(false);api.get('/auth/me').then(r=>setUser(r.data.user)).catch(()=>localStorage.removeItem('ft_token')).finally(()=>setLoading(false))},[]);const login=async d=>{const r=await api.post('/auth/login',d);localStorage.setItem('ft_token',r.data.token);setUser(r.data.user)};const signup=async d=>{const r=await api.post('/auth/signup',d);localStorage.setItem('ft_token',r.data.token);setUser(r.data.user)};const logout=()=>{localStorage.removeItem('ft_token');setUser(null)};return <C.Provider value={{user,loading,login,signup,logout}}>{children}</C.Provider>};export const useAuth=()=>useContext(C);
